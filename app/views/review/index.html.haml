= form_tag 'review_path', method: :get do
    .form-group
        = text_field_tag :query, params[:query], input_html: { class: "form-control" }
    = submit_tag "Search", input_html: { class: "btn btn-primary" }

= render "show_trades"