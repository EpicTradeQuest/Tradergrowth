= flash_messages

- unless trial_expired?
    = render 'form_index'
- if trial_expired? && @subscribed
    = render 'form_index'
- elsif trial_expired?
    = render 'trial_expired'
- else
    = render 'form_index'

= render 'show_trades'